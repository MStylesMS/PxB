{
  "name": "pxb",
  "version": "1.0.0",
  "description": "Paradox Bridge — Z-Wave / Zigbee / Thread to MQTT bridge",
  "main": "src/index.js",
  "bin": {
    "pxb": "src/cli/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --runInBand --testTimeout=20000",
    "test:unit": "jest test/unit --runInBand --testTimeout=20000",
    "lint": "eslint src test",
    "postinstall": "patch-package"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ini": "^4.1.3",
    "mqtt": "^5.10.0",
    "zigbee-herdsman": "^3.0.0",
    "zwave-js": "^15.21.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jest": "^29.7.0",
    "patch-package": "^8.0.1"
  },
  "license": "AGPL-3.0-or-later",
  "private": true
}
